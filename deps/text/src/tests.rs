#![cfg(test)]

use std::ops::Deref;

use crate::Font;

#[test]
fn font() {
    Font::disable_render();
    let font = Font::helvetica();
    dbg!(&font.deref());
}

#[test]
fn font2() {
    Font::disable_render();
    let font = Font::helvetica();
    dbg!(&font.deref());
}

#[test]
fn font3() {
    Font::disable_render();
    let font = Font::helvetica();
    dbg!(&font.deref());
}

#[test]
fn font4() {
    Font::disable_render();
    let font = Font::helvetica();
    dbg!(&font.deref());
}

#[test]
fn font5() {
    Font::disable_render();
    let font = Font::helvetica();
    dbg!(&font.deref());
}

#[test]
fn font6() {
    Font::disable_render();
    let font = Font::helvetica();
    dbg!(&font.deref());
}

#[test]
fn font7() {
    Font::disable_render();
    let font = Font::helvetica();
    dbg!(&font.deref());
}

#[test]
fn font8() {
    Font::disable_render();
    let font = Font::helvetica();
    dbg!(&font.deref());
}

#[test]
fn font9() {
    Font::disable_render();
    let font = Font::helvetica();
    dbg!(&font.deref());
}

#[test]
fn font10() {
    Font::disable_render();
    let font = Font::helvetica();
    dbg!(&font.deref());
}
