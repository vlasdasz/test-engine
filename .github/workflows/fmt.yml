name: Typos

on:
  push:
    branches: ["main"]
  pull_request:
    branches: ["main"]

jobs:
  typos:
    name: Formatting Check
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Actions
        uses: actions/checkout@v4

      - name: Fmt
        run: make fmt-check
